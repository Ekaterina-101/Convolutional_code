{
  "output_file": "data/output.log",
  // Путь к файлу логов. Если не указан, имя формируется автоматически: data/output_<times>.log
  "CRC_polynomial": 8,
  // Степень CRC: 8, 16 или 24 бита
  "CRC_block": 0,
  // Блок проверки CRC: 0 — кодируем слово целиком, > 0 — длина блока
  "modulation": 4,
  // Тип модуляции: 4 = QPSK, 16 = QAM—16, 64 = QAM—64, 256 = QAM—256
  "k": 10,
  // Длина информационного слова
  "information_word": 0,
  // Генерация входных данных: 0 = нулевое слово (все биты 0), 1 = случайная последовательность
  "mode": 0,
  // Режим работы симулятора:
  // 0 — только кодирование
  // 1 — кодирование + модуляция
  // 2 — + канал с шумом + демодуляция
  // 3 — + декодирование (полный цикл)
  // 4 — + расчёт BER/FER (без CRC)
  // 5 — + проверка CRC и расчёт BER/FER
  "trellis_generators": [
    7,
    5
  ],
  // Полиномы свёрточного кода в восьмеричной системе
  // [7,5], [17,15]...
  // [7,7,5], [171,165,133] — стандарт LTE, ...
  "decode_mode": "soft",
  // Алгоритм декодирования:
  // "hard" / "soft" — декодирование Витерби (жёсткие/мягкие решения)
  // "hard_WAVA" / "soft_WAVA" — циклически усечённая решётка (Wrap—Around Viterbi)
  // "hard_parallel_LVA" / "soft_parallel_LVA" — параллельное списочное декодирование (List Viterbi Algorithm)
  // "hard_serial_LVA" / "soft_serial_LVA" — последовательное списочное декодирование (List Viterbi Algorithm)
  "WAVA_max_iter": 3,
  // Максимальное число итераций (проходов по решётке) для алгоритма WAVA
  "LVA_L": 5,
  // Размер списка кандидатов (число лучших путей) для алгоритмов LVA 
  "channel": "AWGN",
  // Канал связи: "AWGN" — канал с АБГШ, "RYLV" — рэлеевский канал
  "channel_seed": 12345,
  // Seed для ГСЧ при моделировании шума
  "EbN0_dB": [
    1.0,
    1.5,
    3.0
  ],
  // Массив значений отношения сигнал/шум (Eb/N0) в дБ
  "attempts": [
    1000,
    500,
    200
  ],
  // Количество экспериментов для каждого значения Eb/N0. 
  // Должно соответствовать длине массива EbN0_dB при simulation_mode = 1
  "simulation_mode": 0,
  // Моделирования:
  // 0 = число попыток для всех точек равно используется attempts[0]
  // 1 = разное число попыток для каждой точки (длина массива attempts == длине EbN0_dB)
  "write_output_BER/FER_file": 0,
  // Экспорт результатов: 0 = только в общий лог, 1 = дополнительно в отдельный txt—файл
  "draw_BER/FER": 0
  // Визуализация: 0 = без графиков, 1 = построить и сохранить графики зависимости BER/FER от Eb/N0
}